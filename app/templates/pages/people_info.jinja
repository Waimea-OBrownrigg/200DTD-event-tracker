{% extends 'pages/base.jinja' %}


{% block title %}

    Person Info

{% endblock %}

{% block nav %}

    <h1>{{person.name}}</h1>

{% endblock %}

{% block content %}

    <p>{{person.availability}}</p>

    <p>
        {% for event in events %}

            {% if event.people_id == person.id %}
                <span class="comma">{{ event.name }}</span>
            {% endif %}

        {% else %}

            None

        {% endfor %}
    </p>

    <a href="/people_list" role="button">Return</a>

{% endblock %}